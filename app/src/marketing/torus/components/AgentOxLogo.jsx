import BrandLogo from "../../../shared/ui/BrandLogo";

/** Marketing / Torus brand mark — uses the official AgentOX logo asset. */
export function AgentOxLogo({ size = 44, className = "" }) {
  return <BrandLogo size={size} className={className} alt="" />;
}
